export const enum Setting {
  filmCardsNumber = 20,
};

export enum AppRoute {
  Login = '/login',
  
}

export const enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
